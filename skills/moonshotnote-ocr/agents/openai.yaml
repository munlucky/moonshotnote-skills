interface:
  display_name: "MoonshotNote OCR"
  short_description: "Korean OCR with ebook page routing"
  default_prompt: "Use $moonshotnote-ocr to extract Korean and English text from this image, use ebook page-mode routing when useful, write OCR outputs, and verify low-confidence lines against image evidence before closing."

interface:
  display_name: "Backend Architecture"
  short_description: "Backend architecture graph with DDD, data/runtime patterns, FastAPI, and Spring mapping"
  default_prompt: "Use $backend-architecture to reason about backend layers, dependency direction, repository/service/use-case boundaries, DDD subdomains, bounded contexts, aggregates, framework leak risks, coupling, cohesion, and FastAPI/Spring adapter placement."
